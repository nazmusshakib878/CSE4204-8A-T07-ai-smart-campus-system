# API Collection

Import `CSE4204-8A-T07_APICollection.json` into Postman to explore the documented API requests. Update the collection's base URL to match the local Laravel server, normally `http://127.0.0.1:8000`.
